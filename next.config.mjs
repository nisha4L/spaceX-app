/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains : ["cloudflare-ipfs.com" ,"avatars.githubusercontent.com","25uwea5voazhoq2l.public.blob.vercel-storage.com"]
    }
};

export default nextConfig;
